interface:
  display_name: "Midtrans - Node.js"
  short_description: "Snap, Core API, Snap BI, Iris disbursement, subscriptions, and tokenization."
  icon_small: "./assets/midtrans.png"
  icon_large: "./assets/midtrans.png"
  brand_color: "#307FC1"
  default_prompt: "Create a Snap payment page for an order with item details, customer info, and a success redirect URL, then handle the HTTP notification callback to update the order status in my database using the midtrans-client npm package."

policy:
  allow_implicit_invocation: true
