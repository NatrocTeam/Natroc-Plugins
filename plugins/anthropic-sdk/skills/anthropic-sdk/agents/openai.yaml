interface:
  display_name: "Anthropic SDK"
  short_description: "Route Anthropic SDK work to development docs or existing-code validation docs for Client SDK and Claude Agent SDK."
  default_prompt: "Use Anthropic SDK docs to inspect this project, choose development or validation mode, choose the right SDK, and verify the result."
policy:
  allow_implicit_invocation: true
