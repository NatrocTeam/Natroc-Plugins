interface:
  display_name: "Anthropic Client SDK"
  short_description: "Build and debug direct Claude API integrations with @anthropic-ai/sdk or anthropic."
  default_prompt: "Use the Anthropic Client SDK docs to implement direct Claude API calls, streaming, tools, structured output, or validation."
policy:
  allow_implicit_invocation: true
