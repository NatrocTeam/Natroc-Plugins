interface:
  display_name: "Claude Agent SDK"
  short_description: "Build and debug programmatic Claude Code agents with TypeScript or Python."
  default_prompt: "Use the Claude Agent SDK docs to implement query(), ClaudeSDKClient, tools, hooks, MCP, sessions, and permissions."
policy:
  allow_implicit_invocation: true
