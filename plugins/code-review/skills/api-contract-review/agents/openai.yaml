interface:
  display_name: "API Contract Review"
  short_description: "Audit frontend-backend API contracts: request/response schemas, status codes, errors, pagination, filtering, sorting, versioning, and timezone."
  default_prompt: "Use the api-contract-review skill for its documented code-review scope. Follow evidence policy, docs grounding, and fix gates."
policy:
  allow_implicit_invocation: false
