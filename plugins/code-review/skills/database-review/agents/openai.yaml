interface:
  display_name: "Database Review"
  short_description: "Audit database schemas, migrations, constraints, indexes, transactions, race conditions, query safety, N+1 risks, and consistency."
  default_prompt: "Use the database-review skill for its documented code-review scope. Follow evidence policy, docs grounding, and fix gates."
policy:
  allow_implicit_invocation: false
