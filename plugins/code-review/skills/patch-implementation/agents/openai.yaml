interface:
  display_name: "Patch Implementation"
  short_description: "Apply minimal safe patches only after explicit approval, preserving public API and avoiding unrelated refactors or dependency additions."
  default_prompt: "Use the patch-implementation skill for its documented code-review scope. Follow evidence policy, docs grounding, and fix gates."
policy:
  allow_implicit_invocation: false
