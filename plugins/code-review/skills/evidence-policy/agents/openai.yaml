interface:
  display_name: "Evidence Policy"
  short_description: "Enforce strict finding requirements: file evidence, docs basis, impact, suggested fix, confidence, and unverified classification."
  default_prompt: "Use the evidence-policy skill for its documented code-review scope. Follow evidence policy, docs grounding, and fix gates."
policy:
  allow_implicit_invocation: false
