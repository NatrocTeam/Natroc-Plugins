interface:
  display_name: "Code Review - Regression Test Generation"
  short_description: "Generate regression tests for fixed issues."
  icon_small: "./assets/logo.svg"
  icon_large: "./assets/logo.svg"
  brand_color: "#2563EB"
  default_prompt: "Generate regression tests for fixed bugs, security issues, or API contracts. Add tests that reproduce the original failure and verify the fix. Use the existing test framework and follow project conventions."

policy:
  allow_implicit_invocation: true
