interface:
  display_name: "Code Review - Codebase Discovery"
  short_description: "Identify repo stack, structure, and entry points."
  icon_small: "./assets/logo.svg"
  icon_large: "./assets/logo.svg"
  brand_color: "#2563EB"
  default_prompt: "Explore the repository to identify its tech stack, project structure, config files, entry points, package manager, framework, test setup, database, and deployment configuration before starting a review."

policy:
  allow_implicit_invocation: true
