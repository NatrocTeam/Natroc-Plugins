interface:
  display_name: "Code Review - Fix Planning"
  short_description: "Create safe fix plans from review findings."
  icon_small: "./assets/logo.svg"
  icon_large: "./assets/logo.svg"
  brand_color: "#2563EB"
  default_prompt: "Create a safe fix plan for the identified findings. For each finding, specify the root cause, files to change, risk assessment, documentation basis, required tests, verification steps, and rollback strategy."

policy:
  allow_implicit_invocation: true
