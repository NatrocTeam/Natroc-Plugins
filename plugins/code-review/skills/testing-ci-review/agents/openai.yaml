interface:
  display_name: "Code Review - Testing and CI Review"
  short_description: "Review tests, CI workflows, and verification commands."
  icon_small: "./assets/logo.svg"
  icon_large: "./assets/logo.svg"
  brand_color: "#2563EB"
  default_prompt: "Review the test suite and CI configuration. Check test coverage of critical paths, lint and typecheck setup, CI workflow definitions, regression test coverage, and verification command correctness."

policy:
  allow_implicit_invocation: true
