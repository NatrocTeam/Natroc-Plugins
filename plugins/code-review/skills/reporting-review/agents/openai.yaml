interface:
  display_name: "Code Review - Reporting Review"
  short_description: "Deduplicate findings and enforce report schema."
  icon_small: "./assets/logo.svg"
  icon_large: "./assets/logo.svg"
  brand_color: "#2563EB"
  default_prompt: "Review and clean up the findings report. Deduplicate similar findings, order by severity, enforce the report schema, separate evidence from interpretation, set clear verdicts, and list fix options for each finding."

policy:
  allow_implicit_invocation: true
