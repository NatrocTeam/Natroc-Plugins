interface:
  display_name: "React TS Types"
  short_description: "Type-safe React props, hooks, refs, events, context, and reusable component APIs."
  default_prompt: "Use $react-typescript-types to create or review idiomatic TypeScript types for React components."
