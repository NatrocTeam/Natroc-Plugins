interface:
  display_name: "React TypeScript"
  short_description: "Check all files that use React Typescript or scoped-path that use React Typescript"
  default_prompt: "Use $react-typescript to build or review React TypeScript code with the bundled local references."

policy:
  allow_implicit_invocation: true
