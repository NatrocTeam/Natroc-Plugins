interface:
  display_name: "React TypeScript - Patterns"
  short_description: "Reusable React patterns with polymorphic props, discriminated unions, and generic components."
  default_prompt: "Design a reusable React TypeScript component pattern with proper polymorphic props, discriminated unions, or generic constraints following the bundled patterns references."
  brand_color: "#3178C6"

policy:
  allow_implicit_invocation: true
