interface:
  display_name: "React TypeScript - Hooks"
  short_description: "Type React hooks including state, reducers, context, refs, and custom hooks."
  default_prompt: "Type these React hooks, reducer actions, context providers, and refs with proper TypeScript following the bundled hooks references."
  brand_color: "#3178C6"

policy:
  allow_implicit_invocation: true
