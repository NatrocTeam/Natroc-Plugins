interface:
  display_name: "Expo - CI/CD Workflows"
  short_description: "Write and validate EAS workflow YAML files for Expo builds and deployments."
  icon_small: "./assets/expo.png"
  icon_large: "./assets/expo.png"
  brand_color: "#000020"
  default_prompt: "Write an EAS workflow YAML that triggers on push to main, runs a production iOS build with a development client profile, uploads the artifact, and notifies a Slack channel on completion or failure."

policy:
  allow_implicit_invocation: true
