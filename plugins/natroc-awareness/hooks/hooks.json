{
  "description": "Load compact Natroc plugin routing context at session start.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/session_start_context.py",
            "timeout": 10,
            "statusMessage": "Loading Natroc plugin awareness"
          }
        ]
      }
    ]
  }
}
